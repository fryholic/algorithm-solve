#include <iostream>
#include <cstring>

using namespace std;

int main(){
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    
    char A[101];
    cin >> A;
    int len = strlen(A);
    cout << len;

    return 0;
}