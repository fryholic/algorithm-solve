#include <iostream>
#include <string>

using namespace std;

int main(){
    //cin.tie(NULL);
    //ios::sync_with_stdio(false);
    
    char c;

    while(scanf("%c", &c) != EOF){
        printf("%c", c);
    }

    return 0;
}